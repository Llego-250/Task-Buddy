spring.application.name=task-buddy-backend
server.port=8080

# MongoDB connection (adjust URI as needed)
spring.data.mongodb.uri=mongodb://localhost:27017/taskbuddy

# Optional: database name can be set separately
spring.data.mongodb.database=taskbuddy

-- Run this once to create the database
-- Connect as superuser (e.g. psql -U postgres) then run:

CREATE DATABASE taskbuddy;

-- Tables are auto-created by Hibernate (ddl-auto=update)
-- To connect: jdbc:postgresql://localhost:5432/taskbuddy
--             username: postgres  |  password: postgres
--
-- To change credentials, update:
--   task-buddy-backend/src/main/resources/application.properties
